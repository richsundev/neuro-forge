# Template only — placeholder values. Generate real ones and apply out-of-band:
#   kubectl create secret generic neuroforge-secrets -n neuroforge \
#     --from-literal=NEUROFORGE_DATABASE_URL='postgresql+psycopg2://user:pass@host:5432/neuroforge' \
#     --from-literal=POSTGRES_PASSWORD='...' \
#     --from-literal=NEUROFORGE_BOOTSTRAP_API_KEY='...'
# Never `kubectl apply -f` this file as-is in a real cluster.
apiVersion: v1
kind: Secret
metadata:
  name: neuroforge-secrets
  namespace: neuroforge
type: Opaque
stringData:
  NEUROFORGE_DATABASE_URL: "postgresql+psycopg2://neuroforge:CHANGE_ME@neuroforge-postgres:5432/neuroforge"
  POSTGRES_PASSWORD: "CHANGE_ME"
  NEUROFORGE_BOOTSTRAP_API_KEY: "CHANGE_ME"
